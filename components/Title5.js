import React, {Fragment,useState} from "react"
import ReactDOM from "react-dom"

const Title5 =(props)=>{
return (<h5 className={props.className} id={props.id}>{props.text}</h5>)
}

export default Title5;