import React, {Fragment,useState} from "react"
import ReactDOM from "react-dom"

const Title4 =(props)=>{
return (<h4 className={props.className} id={props.id}>{props.text}</h4>)
}

export default Title4;